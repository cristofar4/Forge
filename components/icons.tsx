import { Factory, Activity, Truck, Shield, Building2, Zap, type LucideIcon } from "lucide-react";

/** Maps solution icon keys to lucide components. */
export const solutionIcons: Record<string, LucideIcon> = {
  factory: Factory,
  activity: Activity,
  truck: Truck,
  shield: Shield,
  building: Building2,
  zap: Zap,
};
